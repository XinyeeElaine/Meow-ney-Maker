# Copy to .env and fill in. Both values are safe to expose in the client bundle
# (the anon key is the publishable key) ONLY IF Row Level Security is enabled
# on every Supabase table. Verify that before deploying.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
